# @typst-wasm/vite-plugin-typst

A Vite plugin for compiling Typst documents to HTML.

## Install

```sh
npm install @typst-wasm/vite-plugin-typst typst-wasm
```

[Read the documentation](https://typst-wasm.github.io/typst-wasm/packages/vite-plugin-typst/).
