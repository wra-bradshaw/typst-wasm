{
  "name": "@typst-wasm/fonts",
  "version": "0.0.2",
  "type": "module",
  "main": "./index.js",
  "types": "./index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wra-bradshaw/typst-wasm.git"
  },
  "bugs": {
    "url": "https://github.com/wra-bradshaw/typst-wasm/issues"
  },
  "homepage": "https://github.com/wra-bradshaw/typst-wasm#readme",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.js"
    }
  },
  "scripts": {
    "build": "out_path=$(nix build ../..#fonts --no-link --print-out-paths) && rm -rf dist/files && mkdir -p dist/files && cp -R \"$out_path/dist/files/.\" dist/files",
    "fix": "pnpm exec prettier . --write",
    "format": "pnpm exec prettier . --check",
    "format:fix": "pnpm exec prettier . --write"
  },
  "files": [
    "index.js",
    "index.d.ts",
    "dist"
  ],
  "devDependencies": {
    "prettier": "^3.8.4"
  }
}
