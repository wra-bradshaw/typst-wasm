# typst-wasm

Compile Typst documents with WebAssembly in JavaScript runtimes.

## Install

```sh
npm install typst-wasm @typst-wasm/fonts
```

[Read the documentation](https://typst-wasm.github.io/typst-wasm/packages/typst-wasm/getting-started/).
