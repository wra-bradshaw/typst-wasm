{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "flake.nix",
    "flake.lock",
    "package.json",
    "pnpm-lock.yaml",
    "pnpm-workspace.yaml",
    "scripts/**"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".cargo/**",
        "Cargo.toml",
        "Cargo.lock",
        "package.nix",
        "flake-module.nix",
        "scripts/**",
        "src/**"
      ],
      "outputs": ["dist/**", ".next/**", ".output/**"]
    },
    "lint": {
      "dependsOn": ["build"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "test:unit": {
      "dependsOn": ["build"]
    },
    "format": {
      "dependsOn": []
    },
    "fix": {
      "dependsOn": [],
      "cache": false
    },
    "test:integration": {
      "dependsOn": ["^build", "build"]
    },
    "test:browser": {
      "dependsOn": ["^build", "build"],
      "cache": false
    }
  }
}
